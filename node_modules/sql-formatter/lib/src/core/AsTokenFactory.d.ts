import type { KeywordCase } from "../types";
import { type Token } from './token';
export default class AsTokenFactory {
    private keywordCase;
    private detectedCase;
    constructor(keywordCase: KeywordCase, tokens?: Token[]);
    private autoDetectCase;
    /** Returns AS token with either upper- or lowercase text */
    token(): Token;
    private asTokenValue;
}
